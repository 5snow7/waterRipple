
function setup() {
row=500;col=600;rectMode(CENTER);
can=createCanvas(600,500);
can.position(10,10);
can.class("bdd");
start(cur);
refig(cur);
paint(cur);
}

function draw() {
  //background(250,250,0);
//test(cur);
//SSSpaint(cur);
		}

 
 
  
